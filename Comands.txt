# !≈сли команда нечего не вывела она завершилась успешно
# !Git не отслеживает пустые папки (пустые папки нельз€ закомитить)
# !‘айлы в git могут находитс€ в 4 состо€ни€
#  	Untracked(несопровождаемый) - не учитываетс€ в системе git
#  	Unmodified(без изменений)
#   	Modefied(с изменени€ми)
#	Staged(подготовленный) - готов к комиту
# ! ѕоследовательность действий добавлени€ файла на сервер
#	git add .
#	git commit -m 'comment'	
#	git push
# ! »менование комитов чтото вроде #<проект>-<номер задачи> <описание>  #SHOP-1251 »спрален пересчет в корзине заказа

# «адать им€ пользовател€ дл€ всех репозиториев
git config -- global user.name "Gavamot"

# »нициализировать репозиторий (создать скрытую папку .git)
git init

# ѕривести файл к состо€нию Staged ( '.' - все файлы либо им€ конкретного файла)
git add .

# «алить изменени€ на сервер
git push

# ѕосмотреть изменени€
git status

# ѕосмотреть историю изменений
git log

# ќткат репозитори€ к состо€нию после последнего комита
git reset HEAD

# ¬ернуть файлы к поледнему состо€нию
git checkout -- Comands.txt

# ќткат к указанному изменению с потерей всей истории
git reset --hard <commit> #(<commit> - HEAD~3) - откат на 3 изменеи€ назад (чтобы откатитс€ к текущему git reset --hard HEAD)

# ƒобавл€ет к текущему репозиторию ссылку на удаленный под каким то именем
git remote add repName "C:\rep\qq"

# ¬озмет информацию о все изменени€х и перетащих в наш репозиторий (узнать все об удаленном репозитории)
git fetch repName